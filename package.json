{
  "name": "vite-template-solid",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "start": "npm run server && serve dist -s -n -L -p $PORT",
    "dev": "npm run server && vite",
    "build": "npm run server && vite build",
    "serve": "npm run server && vite preview",
    "server": "node server/server.js"
  },
  "license": "MIT",
  "devDependencies": {
    "solid-devtools": "^0.29.2",
    "typescript": "^5.3.3",
    "vite": "^5.0.11",
    "vite-plugin-solid": "^2.8.2"
  },
  "dependencies": {
    "solid-js": "^1.8.11"
  }
}
